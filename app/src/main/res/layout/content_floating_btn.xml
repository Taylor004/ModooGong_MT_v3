<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="bottom|right"
    android:gravity="center"
    android:id="@+id/fab"
    android:visibility="gone">

    <LinearLayout
        android:id="@+id/fabDefault"
        android:layout_width="@dimen/common_target_size"
        android:layout_height="@dimen/common_target_size"
        android:layout_margin="@dimen/fab_margin"
        android:gravity="center"
        android:orientation="vertical"
        android:background="@drawable/floating_icon"
        android:elevation="@dimen/common_fab_elevation">
        <ImageView
            android:id="@+id/imgFabArrow"
            android:layout_width="wrap_content"
            android:layout_height="@dimen/common_fab_icon_size"
            android:src="@drawable/ic_up_up"/>
        <TextView
            android:id="@+id/txtDownCnt"
            style="@style/textLarge"
            android:textColor="@color/white"
            android:includeFontPadding="false"/>
        <ImageView
            android:layout_width="@dimen/common_fab_icon_size"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_up_down"/>
    </LinearLayout>

    <ImageView
        android:id="@+id/fabPause"
        android:layout_width="@dimen/common_target_size"
        android:layout_height="@dimen/common_target_size"
        android:layout_margin="@dimen/fab_margin"
        android:background="@drawable/ic_down_pause"
        android:visibility="gone"/>

</FrameLayout>